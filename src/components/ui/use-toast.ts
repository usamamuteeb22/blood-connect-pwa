
// This is a re-export file that should point to the correct location
// The implementation is in src/hooks/use-toast.ts, not in components/ui/toast

import { useToast, toast } from "@/hooks/use-toast";

export { useToast, toast };
