
// This component is no longer needed as per requirements
// It's kept as an empty component for now to prevent breaking any imports

const EmergencyRequest = ({ onEmergencyRequest }: { onEmergencyRequest: () => void }) => {
  return null;
};

export default EmergencyRequest;
